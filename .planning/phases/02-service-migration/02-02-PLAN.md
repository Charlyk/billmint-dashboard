---
phase: 02-service-migration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/services/user.service.ts
  - src/lib/services/auth.service.ts
  - src/lib/services/timer.service.ts
  - src/lib/services/project.service.ts
  - src/lib/services/logo.service.ts
autonomous: true

must_haves:
  truths:
    - "user.service.ts has zero console.error or console.log calls"
    - "auth.service.ts has zero console.error or console.log calls"
    - "timer.service.ts has zero console.error or console.log calls"
    - "project.service.ts has zero console.error or console.log calls"
    - "logo.service.ts has zero console.error or console.log calls"
    - "All error logs include sanitized error context via sanitizeError()"
    - "TypeScript compilation passes with zero errors"
  artifacts:
    - path: "src/lib/services/user.service.ts"
      provides: "Structured logging for all 11 console calls"
      contains: "createServiceLogger"
    - path: "src/lib/services/auth.service.ts"
      provides: "Structured logging for all 7 console calls"
      contains: "createServiceLogger"
    - path: "src/lib/services/timer.service.ts"
      provides: "Structured logging for all 6 console calls"
      contains: "createServiceLogger"
    - path: "src/lib/services/project.service.ts"
      provides: "Structured logging for all 4 console calls"
      contains: "createServiceLogger"
    - path: "src/lib/services/logo.service.ts"
      provides: "Structured logging for all 4 console calls"
      contains: "createServiceLogger"
  key_links:
    - from: "src/lib/services/user.service.ts"
      to: "src/lib/logging/logger.ts"
      via: "import createServiceLogger"
      pattern: "import.*createServiceLogger.*from.*logging/logger"
    - from: "src/lib/services/auth.service.ts"
      to: "src/lib/logging/logger.ts"
      via: "import createServiceLogger"
      pattern: "import.*createServiceLogger.*from.*logging/logger"
---

<objective>
Migrate five medium-to-high-volume service files (user: 11, auth: 7, timer: 6, project: 4, logo: 4) from console.error/console.log to structured Axiom logging.

Purpose: These five files account for 32 of 72 console calls (44%). Combined with Plan 01, this completes migration of 59/72 calls (82%).

Output: All five service files use createServiceLogger with sanitizeError, zero remaining console calls.
</objective>

<execution_context>
@/Users/albueduard/.claude/get-shit-done/workflows/execute-plan.md
@/Users/albueduard/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-logging-foundation/01-01-SUMMARY.md

@src/lib/logging/logger.ts
@src/lib/logging/sanitizers.ts
@src/lib/services/user.service.ts
@src/lib/services/auth.service.ts
@src/lib/services/timer.service.ts
@src/lib/services/project.service.ts
@src/lib/services/logo.service.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate user.service.ts (11 calls) and auth.service.ts (7 calls)</name>
  <files>src/lib/services/user.service.ts, src/lib/services/auth.service.ts</files>
  <action>
Replace all 18 console.error calls across user.service.ts and auth.service.ts with structured Axiom logging.

**For user.service.ts (11 console.error calls):**

1. Add imports at top of file:
   ```typescript
   import { createServiceLogger } from '@/lib/logging/logger'
   import { sanitizeError } from '@/lib/logging/sanitizers'
   ```

2. Create service logger: `const log = createServiceLogger('user')`

3. Replace each console.error with log.error:
   - Primary pattern: RPC errors on user CRUD operations and auth operations
   - Extract operation name from existing message prefix (e.g., `[User] update_profile RPC error` -> operation: 'update_profile')
   - Use sanitizeError(error) for all error objects
   - Add context: userId (if available), operation name

**For auth.service.ts (7 console.error calls):**

1. Add imports at top of file:
   ```typescript
   import { createServiceLogger } from '@/lib/logging/logger'
   import { sanitizeError } from '@/lib/logging/sanitizers'
   ```

2. Create service logger: `const log = createServiceLogger('auth')`

3. Replace each console.error with log.error:
   - Primary patterns: Email send failures (welcome, verification, password reset) and auth verification errors
   - Preserve email type context in structured fields
   - Use sanitizeError(error) for all error objects
   - Add context: operation type, email type

Do NOT change any business logic, error handling flow, or throw statements. Only replace the logging calls.
  </action>
  <verify>
Run: `grep -c 'console\.\(error\|log\|warn\)' src/lib/services/user.service.ts` — must return 0.
Run: `grep -c 'console\.\(error\|log\|warn\)' src/lib/services/auth.service.ts` — must return 0.
Run: `grep -c 'createServiceLogger' src/lib/services/user.service.ts` — must return 1.
Run: `grep -c 'createServiceLogger' src/lib/services/auth.service.ts` — must return 1.
Run: `npx tsc --noEmit` — must pass with zero errors.
  </verify>
  <done>user.service.ts (11 calls) and auth.service.ts (7 calls) have zero console calls, use scoped service loggers, all errors use sanitizeError().</done>
</task>

<task type="auto">
  <name>Task 2: Migrate timer.service.ts (6), project.service.ts (4), logo.service.ts (4)</name>
  <files>src/lib/services/timer.service.ts, src/lib/services/project.service.ts, src/lib/services/logo.service.ts</files>
  <action>
Replace all 14 console.error calls across timer.service.ts, project.service.ts, and logo.service.ts with structured Axiom logging.

**For each file, apply the same pattern:**

1. Add imports at top:
   ```typescript
   import { createServiceLogger } from '@/lib/logging/logger'
   import { sanitizeError } from '@/lib/logging/sanitizers'
   ```

2. Create service logger with correct service name:
   - timer.service.ts: `const log = createServiceLogger('timer')`
   - project.service.ts: `const log = createServiceLogger('project')`
   - logo.service.ts: `const log = createServiceLogger('logo')`

3. Replace each console.error with log.error:

   **timer.service.ts (6 calls):** RPC errors on timer operations (start, stop, pause, resume, discard, sync). Extract operation from message. Add timerId if available.

   **project.service.ts (4 calls):** RPC errors on project CRUD operations. Extract operation from message. Add projectId if available.

   **logo.service.ts (4 calls):** Storage upload/delete errors. These are Supabase storage operations, not RPC calls. Preserve the storage operation context (upload, delete, URL generation). Add relevant context like fileName, bucketName if available in scope.

Do NOT change any business logic, error handling flow, or throw statements. Only replace the logging calls.
  </action>
  <verify>
Run: `grep -c 'console\.\(error\|log\|warn\)' src/lib/services/timer.service.ts` — must return 0.
Run: `grep -c 'console\.\(error\|log\|warn\)' src/lib/services/project.service.ts` — must return 0.
Run: `grep -c 'console\.\(error\|log\|warn\)' src/lib/services/logo.service.ts` — must return 0.
Run: `npx tsc --noEmit` — must pass with zero errors.
  </verify>
  <done>timer.service.ts (6 calls), project.service.ts (4 calls), logo.service.ts (4 calls) all have zero console calls, use scoped service loggers, all errors use sanitizeError().</done>
</task>

</tasks>

<verification>
1. `grep -rn 'console\.\(error\|log\|warn\)' src/lib/services/user.service.ts src/lib/services/auth.service.ts src/lib/services/timer.service.ts src/lib/services/project.service.ts src/lib/services/logo.service.ts` — must return no results
2. Each file has exactly one `createServiceLogger` call with correct service name
3. Each file imports `sanitizeError` from `@/lib/logging/sanitizers`
4. `npx tsc --noEmit` passes with zero errors
5. `npm run build` succeeds
</verification>

<success_criteria>
- 32 console calls replaced with structured Axiom logger across 5 files
- Each file uses correct ServiceName: 'user', 'auth', 'timer', 'project', 'logo'
- All error logs use sanitizeError() for error context
- All logs include operation-specific structured context (operation name, relevant IDs)
- TypeScript compiles and build passes
</success_criteria>

<output>
After completion, create `.planning/phases/02-service-migration/02-02-SUMMARY.md`
</output>
