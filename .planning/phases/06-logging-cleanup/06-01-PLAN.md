---
phase: 06-logging-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/projects/route.ts
  - src/app/api/projects/[id]/route.ts
  - src/app/api/projects/[id]/entries/route.ts
  - src/app/api/time-entries/route.ts
  - src/app/api/time-entries/[id]/route.ts
  - src/app/api/time-entries/bulk/route.ts
  - src/app/api/time-entries/unbilled/route.ts
  - src/app/api/timer/route.ts
  - src/app/api/timer/start/route.ts
  - src/app/api/timer/stop/route.ts
  - src/app/api/timer/pause/route.ts
  - src/app/api/timer/resume/route.ts
  - src/app/api/timer/discard/route.ts
  - src/app/api/timer/sync/route.ts
  - src/app/api/clients/[id]/route.ts
  - src/app/api/invoices/[id]/route.ts
  - src/app/api/invoices/[id]/send/route.ts
  - src/app/api/invoices/[id]/reminder/route.ts
  - src/app/api/invoices/[id]/mark-paid/route.ts
  - src/app/api/invoices/[id]/void/route.ts
  - src/app/api/invoices/[id]/duplicate/route.ts
  - src/app/api/invoices/[id]/pdf/route.ts
  - src/app/api/invoices/stats/route.ts
  - src/app/api/invoices/public/[token]/route.ts
  - src/app/api/invoices/public/[token]/pdf/route.ts
  - src/app/api/users/me/route.ts
  - src/app/api/users/me/settings/route.ts
  - src/app/api/users/me/settings/app/route.ts
  - src/app/api/users/me/settings/billing/route.ts
  - src/app/api/users/me/settings/onboarding/route.ts
  - src/app/api/users/me/logo/route.ts
  - src/app/api/dashboard/route.ts
  - src/app/api/reports/route.ts
  - src/app/api/reports/export/route.ts
  - src/app/api/billing/checkout/route.ts
  - src/app/api/billing/portal/route.ts
  - src/app/api/billing/subscription/route.ts
  - src/app/api/billing/webhook/route.ts
  - src/app/api/billing/invoices/route.ts
  - src/app/api/cron/auto-pause-timers/route.ts
autonomous: true

must_haves:
  truths:
    - "Every CRUD and business API route handler is wrapped with withLogging"
    - "All API requests through these routes log method, path, status code, and response time"
    - "Correlation IDs propagate through all wrapped route handlers"
  artifacts:
    - path: "src/app/api/projects/route.ts"
      provides: "withLogging-wrapped project list and create handlers"
      contains: "withLogging"
    - path: "src/app/api/timer/route.ts"
      provides: "withLogging-wrapped timer handlers"
      contains: "withLogging"
    - path: "src/app/api/users/me/route.ts"
      provides: "withLogging-wrapped user profile handlers"
      contains: "withLogging"
  key_links:
    - from: "src/app/api/*/route.ts"
      to: "src/lib/logging/route-handler.ts"
      via: "import { withLogging }"
      pattern: "withLogging\\(handle"
---

<objective>
Apply the withLogging wrapper to all CRUD, business, and infrastructure API route handlers.

Purpose: Satisfy LOG-06 and LOG-07 — every API route gets automatic request lifecycle logging with correlation ID propagation, method, path, status code, and response time.
Output: 40 route files migrated from direct `export async function` to `withLogging`-wrapped pattern.
</objective>

<execution_context>
@/Users/eduardalbu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/eduardalbu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/lib/logging/route-handler.ts
@src/app/api/clients/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply withLogging to projects, time-entries, timer, clients/[id], and invoices sub-routes</name>
  <files>
    src/app/api/projects/route.ts
    src/app/api/projects/[id]/route.ts
    src/app/api/projects/[id]/entries/route.ts
    src/app/api/time-entries/route.ts
    src/app/api/time-entries/[id]/route.ts
    src/app/api/time-entries/bulk/route.ts
    src/app/api/time-entries/unbilled/route.ts
    src/app/api/timer/route.ts
    src/app/api/timer/start/route.ts
    src/app/api/timer/stop/route.ts
    src/app/api/timer/pause/route.ts
    src/app/api/timer/resume/route.ts
    src/app/api/timer/discard/route.ts
    src/app/api/timer/sync/route.ts
    src/app/api/clients/[id]/route.ts
    src/app/api/invoices/[id]/route.ts
    src/app/api/invoices/[id]/send/route.ts
    src/app/api/invoices/[id]/reminder/route.ts
    src/app/api/invoices/[id]/mark-paid/route.ts
    src/app/api/invoices/[id]/void/route.ts
    src/app/api/invoices/[id]/duplicate/route.ts
    src/app/api/invoices/[id]/pdf/route.ts
    src/app/api/invoices/stats/route.ts
    src/app/api/invoices/public/[token]/route.ts
    src/app/api/invoices/public/[token]/pdf/route.ts
  </files>
  <action>
    For each route file, apply the exact same pattern used in `src/app/api/clients/route.ts`:

    1. Add import: `import { withLogging } from '@/lib/logging/route-handler'`
    2. Rename each `export async function GET/POST/PATCH/PUT/DELETE(...)` to `async function handleGet/handlePost/handlePatch/handlePut/handleDelete(...)`
    3. Add exports at bottom: `export const GET = withLogging(handleGet)`, etc.
    4. Preserve all existing function signatures, parameters, and logic unchanged.

    **Naming convention for handlers:**
    - GET -> handleGet
    - POST -> handlePost
    - PATCH -> handlePatch
    - PUT -> handlePut
    - DELETE -> handleDelete

    **For routes with `type RouteParams`:** Keep the RouteParams type and pass context through — withLogging already supports the `context?: { params: Promise<Record<string, string>> }` signature.

    **Do NOT modify:** Function bodies, error handling, imports other than adding withLogging.
  </action>
  <verify>
    Run `npx tsc --noEmit` — no TypeScript errors.
    Grep for `export async function (GET|POST|PATCH|PUT|DELETE)` in all modified files — should return 0 matches (all converted to withLogging pattern).
  </verify>
  <done>
    All 25 route files in projects, time-entries, timer, clients/[id], and invoices sub-routes use withLogging wrapper. No direct function exports remain.
  </done>
</task>

<task type="auto">
  <name>Task 2: Apply withLogging to users, dashboard, reports, billing, and cron routes</name>
  <files>
    src/app/api/users/me/route.ts
    src/app/api/users/me/settings/route.ts
    src/app/api/users/me/settings/app/route.ts
    src/app/api/users/me/settings/billing/route.ts
    src/app/api/users/me/settings/onboarding/route.ts
    src/app/api/users/me/logo/route.ts
    src/app/api/dashboard/route.ts
    src/app/api/reports/route.ts
    src/app/api/reports/export/route.ts
    src/app/api/billing/checkout/route.ts
    src/app/api/billing/portal/route.ts
    src/app/api/billing/subscription/route.ts
    src/app/api/billing/webhook/route.ts
    src/app/api/billing/invoices/route.ts
    src/app/api/cron/auto-pause-timers/route.ts
  </files>
  <action>
    Apply the same withLogging pattern as Task 1 to each route file:

    1. Add import: `import { withLogging } from '@/lib/logging/route-handler'`
    2. Rename exports to private handlers, wrap with withLogging.

    **Special case — cron/auto-pause-timers/route.ts:**
    This route already has its own manual request/response logging (startTime, duration, log.info/log.error). Still wrap with withLogging for consistency and correlation ID propagation — the wrapper's logging is complementary (API-level), while the existing logging captures job-specific details (processed, paused, errors count). Do NOT remove the existing cron-specific logging.

    **Special case — billing/webhook/route.ts:**
    Wrap with withLogging. The webhook handler reads raw body with `request.text()` — this is fine since withLogging does not consume the request body.

    **Do NOT modify:** Function bodies, error handling, existing service logger usage.
  </action>
  <verify>
    Run `npx tsc --noEmit` — no TypeScript errors.
    Grep all 15 files for withLogging import — all should match.
    Grep for `export async function` in these files — should return 0 matches.
  </verify>
  <done>
    All 15 route files in users, dashboard, reports, billing, and cron use withLogging wrapper. Combined with Task 1 and already-wrapped clients/invoices routes, 100% of API routes are now instrumented.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with no errors
2. `grep -r "export async function" src/app/api/ --include="route.ts"` returns only auth routes (handled in Plan 02)
3. `grep -r "withLogging" src/app/api/ --include="route.ts" | wc -l` shows 40+ matches (all routes wrapped)
</verification>

<success_criteria>
- Every non-auth API route handler exports through withLogging wrapper
- TypeScript compilation succeeds with no errors
- No direct `export async function GET/POST/PATCH/DELETE` remains in modified files
- All existing route behavior preserved (no function body changes)
</success_criteria>

<output>
After completion, create `.planning/phases/06-logging-cleanup/06-01-SUMMARY.md`
</output>
