---
phase: 06-logging-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/auth/login/route.ts
  - src/app/api/auth/logout/route.ts
  - src/app/api/auth/session/route.ts
  - src/app/api/auth/signup/route.ts
  - src/app/api/auth/google/route.ts
  - src/app/api/auth/reset-password/route.ts
  - src/app/api/auth/verify-email/route.ts
  - src/app/api/auth/callback/route.ts
  - src/lib/analytics/events.ts
  - src/lib/api/auth.ts
autonomous: true

must_haves:
  truths:
    - "Every auth API route handler is wrapped with withLogging"
    - "first_project_created, first_timer_started, first_invoice_sent helpers no longer exist in events.ts"
    - "PostHog First time event filter approach is documented as deliberate decision"
  artifacts:
    - path: "src/app/api/auth/login/route.ts"
      provides: "withLogging-wrapped login handler"
      contains: "withLogging"
    - path: "src/app/api/auth/callback/route.ts"
      provides: "withLogging-wrapped OAuth callback handler"
      contains: "withLogging"
    - path: "src/lib/analytics/events.ts"
      provides: "Clean analytics events without unused first_* helpers"
    - path: "src/lib/api/auth.ts"
      provides: "Updated documentation about PostHog First time event filter"
      contains: "First time event"
  key_links:
    - from: "src/app/api/auth/*/route.ts"
      to: "src/lib/logging/route-handler.ts"
      via: "import { withLogging }"
      pattern: "withLogging\\(handle"
---

<objective>
Apply withLogging to all auth routes and remove unused analytics first_* helpers.

Purpose: Complete LOG-06 coverage for auth routes, satisfy CLN-01 (remove unused first_* helpers), and CLN-02 (document PostHog filter decision).
Output: 8 auth route files wrapped with withLogging, events.ts cleaned up, auth.ts comment refined.
</objective>

<execution_context>
@/Users/eduardalbu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/eduardalbu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/lib/logging/route-handler.ts
@src/app/api/clients/route.ts
@src/lib/analytics/events.ts
@src/lib/api/auth.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply withLogging to all auth routes</name>
  <files>
    src/app/api/auth/login/route.ts
    src/app/api/auth/logout/route.ts
    src/app/api/auth/session/route.ts
    src/app/api/auth/signup/route.ts
    src/app/api/auth/google/route.ts
    src/app/api/auth/reset-password/route.ts
    src/app/api/auth/verify-email/route.ts
    src/app/api/auth/callback/route.ts
  </files>
  <action>
    Apply the withLogging wrapper pattern to each auth route file:

    1. Add import: `import { withLogging } from '@/lib/logging/route-handler'`
    2. Rename each `export async function GET/POST/PATCH/PUT(...)` to `async function handleGet/handlePost/handlePatch/handlePut(...)`
    3. Add exports at bottom: `export const GET = withLogging(handleGet)`, etc.

    **Special case — auth/callback/route.ts:**
    This route uses NextResponse.redirect() instead of Response.json(). This is fine — withLogging just wraps the handler and logs the response status. Redirects return 307/302 status codes which will be logged correctly.

    **Special case — auth/reset-password/route.ts:**
    Has both POST and PATCH handlers. Rename both: handlePost, handlePatch.

    **Special case — auth/verify-email/route.ts:**
    Has both POST and PUT handlers. Rename both: handlePost, handlePut.

    **Do NOT modify:** Function bodies, error handling, existing logic.
  </action>
  <verify>
    Run `npx tsc --noEmit` — no TypeScript errors.
    Grep all 8 auth route files for withLogging import — all should match.
    Grep for `export async function` in these files — should return 0 matches.
  </verify>
  <done>
    All 8 auth route files use withLogging wrapper. Combined with Plan 01, 100% of API routes are now instrumented with withLogging.
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove unused first_* helpers and document PostHog filter decision</name>
  <files>
    src/lib/analytics/events.ts
    src/lib/api/auth.ts
  </files>
  <action>
    **CLN-01 — Remove unused first_* helpers from events.ts:**

    Delete the three unused funnel helper methods from the `analytics` object:
    ```
    firstProjectCreated() { track('first_project_created') },
    firstTimerStarted() { track('first_timer_started') },
    firstInvoiceSent() { track('first_invoice_sent') },
    ```

    Also remove the `// Funnel-ready events (EVT-05)` comment group header above them. Keep the `signupCompleted()` method — it IS used (called in `src/lib/api/auth.ts` signup function).

    The section should end with:
    ```typescript
      // Signup event
      signupCompleted() {
        track('signup_completed')
      },
    ```

    **CLN-02 — Update auth.ts comment to document the deliberate decision:**

    Replace the existing comment block in `src/lib/api/auth.ts` (lines 5-8) with a clearer deliberate-decision comment:
    ```typescript
    // Deliberate decision: first_project_created, first_timer_started, first_invoice_sent
    // funnel events are NOT tracked explicitly. PostHog's built-in "First time event" filter
    // applied to the standard CRUD events (project_created, timer_started, invoice_sent)
    // provides equivalent funnel analysis without unreliable client-side first-time detection.
    ```

    **Do NOT modify:** Any other code in these files.
  </action>
  <verify>
    Run `npx tsc --noEmit` — no TypeScript errors.
    Grep for `first_project_created\|first_timer_started\|first_invoice_sent` in src/ — should only match the comment in auth.ts and nowhere else.
    Verify `signupCompleted` still exists in events.ts.
  </verify>
  <done>
    Three unused first_* helpers removed from events.ts. PostHog "First time event" filter approach documented as deliberate decision in auth.ts. No broken references (helpers were never called).
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with no errors
2. `grep -r "export async function" src/app/api/ --include="route.ts"` returns 0 matches (all routes wrapped)
3. `grep -r "firstProjectCreated\|firstTimerStarted\|firstInvoiceSent" src/` returns 0 matches in .ts files (only in auth.ts comment if at all)
4. `grep -r "signupCompleted" src/lib/analytics/events.ts` still returns a match (not removed)
5. `grep -c "withLogging" src/app/api/ -r --include="route.ts"` shows coverage across all route files
</verification>

<success_criteria>
- All auth route handlers wrapped with withLogging (LOG-06 complete for 100% of routes)
- firstProjectCreated, firstTimerStarted, firstInvoiceSent removed from events.ts (CLN-01)
- PostHog "First time event" filter decision documented in auth.ts comment (CLN-02)
- TypeScript compilation succeeds
- signupCompleted helper preserved (still used)
</success_criteria>

<output>
After completion, create `.planning/phases/06-logging-cleanup/06-02-SUMMARY.md`
</output>
